# lessons_log — 이 저장소에서 한 실수 기록 (1회성은 여기만, 반복되면 규칙으로)

- 2026-09-16 · **마스터를 오버뷰가 아니라 상세 스펙처럼 채움** (Ken 피드백 → 즉시 규칙화) · 헤더 케이스 표·바디 케이스 3장·로딩 placeholder 표·정렬 표를 마스터에 넣었는데, 전부 "부품을 어떻게 만드느냐"의 규칙이라 상세 스펙 몫이었다 · 원인: "디자인 결정이 갈리는 지점"을 화면 단위가 아니라 부품 단위까지 내려가서 셈 · 방지: 지침서 §4-1에 오버뷰/상세 스펙 판별 질문 3개(다른 화면을 그리게 되나 / 부품 제작 규칙인가 / 모든 크기에 나란히 붙나) 추가. **한 크기에만 붙는 케이스 묶음은 오버뷰가 아니라는 신호**(헤더 케이스가 small에만 있었던 것이 그 증상)

- 2026-09-16 · **진입점을 옛 플로우 마커만 보고 확정** (Ken: 고른 3곳이 틀림 → 즉시 규칙화) · 원인: "질문하지 말고 판단하라"는 지시를 "근거가 약해도 확정하라"로 해석. 마커 기준이 파일마다 달랐고(로우 제목 셀 / 더보기 버튼 / 결과 유형) 1곳은 마커 없이 내 추론이었는데 그대로 진입 섹션에 넣음 · 방지: 지침서 §3-1에 3단계(후보 표 → 마커가 실제로 있는 것만 수록 → 사람 한 줄 확인). 프로젝트 문서에 진입 경로 칸이 아예 없음을 전수 확인("진입" 0건)했으므로, 이건 문서로 답이 안 나오는 항목으로 분류

- 2026-09-11 · **문서가 커버하는 데스크톱 1440 화면을 마스터에서 뺌** (Ken 강한 교정 → 즉시 규칙화) · 원인: 라벨 "고정: 1280 사이즈 그대로"만 읽고 1280~ 구간(1440·1680·1920)을 통째로 미수록 판정 · 방지: 지침서 §4-2에 "문서가 커버하는 최대 폭 구간의 대표 1장은 반드시 수록" 추가. 라벨은 구간 안의 중복을 빼는 근거이지 구간 자체를 빼는 근거가 아님

- 2026-09-11 · **1회 시험 결과를 일반 규칙처럼 적음** (`upload_assets` 자동 배치가 "안 된다"고 spike 문서에 씀 → 같은 날 두 번째 시험에선 됨) · 원인: 표본 1개로 결론 · 방지: 도구 동작은 "이번엔 이랬다 + 안전장치"로 쓰고, 두 번 이상 같으면 규칙으로 (deep-clone.md·new-core-master.md 문구 수정)

- 2026-09-10 · **내려받은 시험 이미지가 저장소 루트에 떨어짐** · 원인: 셸에서 비어 있는 변수로 `cd`를 해 작업 폴더가 바뀌지 않은 채 `curl -o`를 실행 · 방지: 스크래치 경로는 변수 대신 절대 경로로 쓰고, 커밋 전 `git status`로 낯선 파일 확인 (이번엔 커밋 전에 옮김)

- 2026-09-10 · **프로젝트 문서를 Core로 잘못 색인** (세션 2, 스텝메이드 `NO7uetAL9Qmk03IXWSaMej`) · 원인: 워크로그의 "스텝메이드 브랜치" 언급만 보고 Core로 가정, 커버 배지(Working)와 페이지 구성(✅ Mobile/✅ Web/📏 운영 가이드)을 확인하지 않음 · 방지: 지침서 §1에 "링크를 받으면 맨 먼저 Cover 배지·페이지 이름으로 Core/프로젝트 구분" 추가, 색인 스크립트에 자동 구분 넣을 것(할 일)
- 2026-09-09 · **REST 403을 "네트워크 차단"으로 단정** (세션 2 워크로그) · 원인: 프록시 403과 Figma scope 403을 구분하지 않음 · 방지: `.claude/rules/network.md`에 403 구분법 추가 (규칙화 완료)
